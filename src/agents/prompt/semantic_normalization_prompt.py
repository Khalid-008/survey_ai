from langchain_core.prompts import ChatPromptTemplate

_SYSTEM = """\
أنت خبير في التحرير النصي ومعالجة البيانات النصية.
"""

_HUMAN = """\
إليك قائمة بعناصر أو كلمات أو جمل قصيرة تم استخراجها من بيانات (مثلاً: مواضيع، أشخاص، أماكن، أو ملاحظات).
المطلوب إنشاء قاموس (Mapping) يربط كل عنصر من القائمة الخام باسم "موحد دقيق" يمثلها بأفضل صيغة ممكنة.
الهدف هو دمج العناصر المتشابهة جداً برغم اختلاف كتابتها (مثل أخطاء إملائية، إضافة/حذف أل التعريف، مترادفات متطابقة وواضحة جداً) تحت نفس المسمى الموحد.

أرجع النتيجة بصيغة JSON فقط عبارة عن Key-Value حيث الـ Key هو العنصر الأصلي تماماً، والـ Value هو العنصر الموحد. 
يجب أن لا يحتوي الرد على أي نصوص إضافية أو markdown fences (```json ... ```):
{{
  "الكلمة": "الكلمة",
  "مصطلح مشابه": "المصطلح الموحد"
}}

القائمة الخام:
{items_json}
"""

semantic_normalization_prompt = ChatPromptTemplate.from_messages([
    ("system", _SYSTEM),
    ("human",  _HUMAN),
])
