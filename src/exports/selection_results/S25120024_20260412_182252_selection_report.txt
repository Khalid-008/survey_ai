SELECTION PIPELINE REPORT
Survey   : S25120024
Generated: 2026-04-12 18:22:52
======================================================================

── QUESTIONS ──


── GENERATED SQL: Query 1 ──
-- What is the count and percentage of each answer option for overall service satisfaction? (Q296)
SELECT 
  sa.answer,
  COUNT(*) AS answer_count,
  ROUND(100.0 * COUNT(*) / (SELECT COUNT(*) FROM survey_answer sa2 
    JOIN survey_question sq2 ON sq2.id = sa2.survey_question_id
    JOIN survey s2 ON s2.id = sq2.survey_id
    WHERE s2.survey_number = 'S25120024' AND sq2.id = 296), 2) AS percentage
FROM survey_answer sa
JOIN survey_question sq ON sq.id = sa.survey_question_id
JOIN survey s ON s.id = sq.survey_id
WHERE s.survey_number = 'S25120024' AND sq.id = 296
GROUP BY sa.answer;

── RESULT: Query 1 ──
answer  answer_count  percentage
     1           139        4.29
     2            36        1.11
     3           111        3.43
     4           123        3.80
     5          2831       87.38

(5 rows)


── GENERATED SQL: Query 2 ──
-- What is the correlation between overall service satisfaction scores and sales representative performance ratings? (Q296, Q297)
SELECT ROUND(
  (COUNT(*) * SUM(a1.v * a2.v) - SUM(a1.v) * SUM(a2.v)) /
  SQRT(
    (COUNT(*) * SUM(a1.v * a1.v) - POW(SUM(a1.v), 2)) *
    (COUNT(*) * SUM(a2.v * a2.v) - POW(SUM(a2.v), 2))
  ), 4
) AS pearson_correlation
FROM (
  SELECT sa.submission_id, CAST(sa.answer AS DECIMAL(3,1)) AS v
  FROM survey_answer sa
  JOIN survey_question sq ON sq.id = sa.survey_question_id
  JOIN survey s ON s.id = sq.survey_id
  WHERE s.survey_number = 'S25120024' AND sq.id = 296
) a1
JOIN (
  SELECT sa.submission_id, CAST(sa.answer AS DECIMAL(3,1)) AS v
  FROM survey_answer sa
  JOIN survey_question sq ON sq.id = sa.survey_question_id
  JOIN survey s ON s.id = sq.survey_id
  WHERE s.survey_number = 'S25120024' AND sq.id = 297
) a2 ON a1.submission_id = a2.submission_id;

── RESULT: Query 2 ──
 pearson_correlation
               0.129

(1 rows)


── GENERATED SQL: Query 3 ──
-- What is the correlation between overall service satisfaction scores and the speed/accuracy of order execution ratings? (Q296, Q298)
SELECT ROUND(
  (COUNT(*) * SUM(a1.v * a2.v) - SUM(a1.v) * SUM(a2.v)) /
  SQRT(
    (COUNT(*) * SUM(a1.v * a1.v) - POW(SUM(a1.v), 2)) *
    (COUNT(*) * SUM(a2.v * a2.v) - POW(SUM(a2.v), 2))
  ), 4
) AS pearson_correlation
FROM (
  SELECT sa.submission_id, CAST(sa.answer AS DECIMAL(3,1)) AS v
  FROM survey_answer sa
  JOIN survey_question sq ON sq.id = sa.survey_question_id
  JOIN survey s ON s.id = sq.survey_id
  WHERE s.survey_number = 'S25120024' AND sq.id = 296
) a1
JOIN (
  SELECT sa.submission_id, CAST(sa.answer AS DECIMAL(3,1)) AS v
  FROM survey_answer sa
  JOIN survey_question sq ON sq.id = sa.survey_question_id
  JOIN survey s ON s.id = sq.survey_id
  WHERE s.survey_number = 'S25120024' AND sq.id = 298
) a2 ON a1.submission_id = a2.submission_id;

── RESULT: Query 3 ──
 pearson_correlation
              0.1496

(1 rows)


── GENERATED SQL: Query 4 ──
-- What proportion of survey submissions have a NULL answer for the recommendation likelihood question? (Q299)
SELECT 
  CASE WHEN sa.answer IS NULL THEN 'NULL' ELSE 'NOT_NULL' END AS answer_status,
  COUNT(*) AS count,
  ROUND(100.0 * COUNT(*) / (SELECT COUNT(*) FROM survey_answer sa2 
    JOIN survey_question sq2 ON sq2.id = sa2.survey_question_id
    JOIN survey s2 ON s2.id = sq2.survey_id
    WHERE s2.survey_number = 'S25120024' AND sq2.id = 299), 2) AS percentage
FROM survey_answer sa
JOIN survey_question sq ON sq.id = sa.survey_question_id
JOIN survey s ON s.id = sq.survey_id
WHERE s.survey_number = 'S25120024' AND sq.id = 299
GROUP BY CASE WHEN sa.answer IS NULL THEN 'NULL' ELSE 'NOT_NULL' END;

── RESULT: Query 4 ──
answer_status  count  percentage
         NULL   3240       100.0

(1 rows)
