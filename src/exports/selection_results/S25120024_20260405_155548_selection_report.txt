SELECTION PIPELINE REPORT
Survey   : S25120024
Generated: 2026-04-05 15:55:48
======================================================================

── QUESTIONS ──


── GENERATED SQL: Query 1 ──
-- ما هو توزيع إجابات السؤال 296 (الرضا العام عن الخدمات) بين جميع المستجيبين؟ (Q296)
SELECT CAST(sa.answer AS SIGNED) AS answer_value, COUNT(*) AS count
FROM survey_answer sa
JOIN survey_question sq ON sq.id = sa.survey_question_id
JOIN survey s ON s.id = sq.survey_id
WHERE s.survey_number = 'S25120024'
  AND sq.id = 296
GROUP BY CAST(sa.answer AS SIGNED)
ORDER BY answer_value;

── RESULT: Query 1 ──
 answer_value  count
            1    139
            2     36
            3    109
            4    121
            5   2813

(5 rows)


── GENERATED SQL: Query 2 ──
-- ما هو المتوسط الحسابي للدرجات لكل من الأسئلة 296، 297، 298؟ (Q296, Q297, Q298)
SELECT sq.id AS question_id, AVG(CAST(sa.answer AS DECIMAL(3,1))) AS average_score
FROM survey_answer sa
JOIN survey_question sq ON sq.id = sa.survey_question_id
JOIN survey s ON s.id = sq.survey_id
WHERE s.survey_number = 'S25120024'
  AND sq.id IN (296, 297, 298)
GROUP BY sq.id
ORDER BY sq.id;

── RESULT: Query 2 ──
 question_id  average_score
         296        4.68832
         297        4.77564
         298        4.72685

(3 rows)


── GENERATED SQL: Query 3 ──
-- ما النسبة المئوية للمستجيبين الذين اختاروا أعلى درجة (5) في السؤال 296؟ (Q296)
SELECT ROUND(100.0 * COUNT(*) / (SELECT COUNT(*) FROM survey_answer sa2
  JOIN survey_question sq2 ON sq2.id = sa2.survey_question_id
  JOIN survey s2 ON s2.id = sq2.survey_id
  WHERE s2.survey_number = 'S25120024' AND sq2.id = 296), 2) AS percentage_5
FROM survey_answer sa
JOIN survey_question sq ON sq.id = sa.survey_question_id
JOIN survey s ON s.id = sq.survey_id
WHERE s.survey_number = 'S25120024'
  AND sq.id = 296
  AND CAST(sa.answer AS SIGNED) = 5;

── RESULT: Query 3 ──
 percentage_5
        87.41

(1 rows)


── GENERATED SQL: Query 4 ──
-- كيف تغير المتوسط الشهري لإجابات السؤال 296 خلال فترة جمع البيانات؟ (Q296)
SELECT DATE_FORMAT(sa.created_date, '%Y-%m') AS month, AVG(CAST(sa.answer AS DECIMAL(3,1))) AS average_score
FROM survey_answer sa
JOIN survey_question sq ON sq.id = sa.survey_question_id
JOIN survey s ON s.id = sq.survey_id
WHERE s.survey_number = 'S25120024'
  AND sq.id = 296
GROUP BY DATE_FORMAT(sa.created_date, '%Y-%m')
ORDER BY month;

── RESULT: Query 4 ──
  month  average_score
2024-04        4.00000
2024-05        4.67442
2024-06        4.79518
2024-07        4.56322
2024-08        4.44203
2024-09        4.77202
2024-10        4.70330
2024-11        4.83186
2024-12        4.44954
2025-01        4.48673
2025-02        4.25000
2025-03        4.76923
2025-04        4.71724
2025-05        4.81176
2025-06        4.80392
2025-07        4.72483
2025-08        4.73469
2025-09        4.56647
2025-10        4.74490
2025-11        4.83333
2025-12        4.53077
2026-01        4.94737
2026-02        4.71429
2026-03        4.83246
2026-04        5.00000

(25 rows)


── GENERATED SQL: Query 5 ──
-- ما هو ارتباط تقييم أداء مندوب المبيعات (السؤال 297) مع تقييم الرضا العام (السؤال 296)؟ (Q296, Q297)
SELECT ROUND(
  (COUNT(*) * SUM(a1.v * a2.v) - SUM(a1.v) * SUM(a2.v)) /
  SQRT(
    (COUNT(*) * SUM(a1.v * a1.v) - POW(SUM(a1.v), 2)) *
    (COUNT(*) * SUM(a2.v * a2.v) - POW(SUM(a2.v), 2))
  ), 4
) AS pearson_correlation
FROM (
  SELECT sa.submission_id, CAST(sa.answer AS DECIMAL(3,1)) AS v
  FROM survey_answer sa
  JOIN survey_question sq ON sq.id = sa.survey_question_id
  JOIN survey s ON s.id = sq.survey_id
  WHERE s.survey_number = 'S25120024' AND sq.id = 297
) a1
JOIN (
  SELECT sa.submission_id, CAST(sa.answer AS DECIMAL(3,1)) AS v
  FROM survey_answer sa
  JOIN survey_question sq ON sq.id = sa.survey_question_id
  JOIN survey s ON s.id = sq.survey_id
  WHERE s.survey_number = 'S25120024' AND sq.id = 296
) a2 ON a1.submission_id = a2.submission_id;

── RESULT: Query 5 ──
 pearson_correlation
              0.1285

(1 rows)


── GENERATED SQL: Query 6 ──
-- ما هو ارتباط تقييم سرعة ودقة التنفيذ (السؤال 298) مع تقييم الرضا العام (السؤال 296)؟ (Q296, Q298)
SELECT ROUND(
  (COUNT(*) * SUM(a1.v * a2.v) - SUM(a1.v) * SUM(a2.v)) /
  SQRT(
    (COUNT(*) * SUM(a1.v * a1.v) - POW(SUM(a1.v), 2)) *
    (COUNT(*) * SUM(a2.v * a2.v) - POW(SUM(a2.v), 2))
  ), 4
) AS pearson_correlation
FROM (
  SELECT sa.submission_id, CAST(sa.answer AS DECIMAL(3,1)) AS v
  FROM survey_answer sa
  JOIN survey_question sq ON sq.id = sa.survey_question_id
  JOIN survey s ON s.id = sq.survey_id
  WHERE s.survey_number = 'S25120024' AND sq.id = 298
) a1
JOIN (
  SELECT sa.submission_id, CAST(sa.answer AS DECIMAL(3,1)) AS v
  FROM survey_answer sa
  JOIN survey_question sq ON sq.id = sa.survey_question_id
  JOIN survey s ON s.id = sq.survey_id
  WHERE s.survey_number = 'S25120024' AND sq.id = 296
) a2 ON a1.submission_id = a2.submission_id;

── RESULT: Query 6 ──
 pearson_correlation
              0.1492

(1 rows)


── GENERATED SQL: Query 7 ──
-- ما هو المتوسط لتقييم سرعة ودقة التنفيذ (السؤال 298) بين المستجيبين الذين أعطوا أعلى درجة (5) في الرضا العام (السؤال 296)؟ (Q296, Q298)
SELECT AVG(CAST(sa298.answer AS DECIMAL(3,1))) AS average_score
FROM survey_answer sa298
JOIN survey_question sq298 ON sq298.id = sa298.survey_question_id
JOIN survey s ON s.id = sq298.survey_id
JOIN survey_answer sa296 ON sa296.submission_id = sa298.submission_id
JOIN survey_question sq296 ON sq296.id = sa296.survey_question_id
WHERE s.survey_number = 'S25120024'
  AND sq298.id = 298
  AND sq296.id = 296
  AND CAST(sa296.answer AS SIGNED) = 5;

── RESULT: Query 7 ──
 average_score
       4.20714

(1 rows)


── GENERATED SQL: Query 8 ──
-- عدد الردود التي تحتوي على قيمة NULL في السؤال 299 (احتمالية التوصية)؟ (Q299)
SELECT COUNT(*) AS null_count
FROM survey_answer sa
JOIN survey_question sq ON sq.id = sa.survey_question_id
JOIN survey s ON s.id = sq.survey_id
WHERE s.survey_number = 'S25120024'
  AND sq.id = 299
  AND sa.answer IS NULL;

── RESULT: Query 8 ──
 null_count
       3218

(1 rows)
